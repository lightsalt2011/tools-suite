作用：
    用来比较手机烧写前后的关键image是否有变化

安装与使用：
    1.windows环境下，需安装cgywin64 termianl终端。直接使用工具包的setup安装程序即可
      linux不用安装

    2. 下载手机上软件版本对应的版本到本地，并将win-cmp-tools包中的两个脚本copy到软件版本包中

    3. 打开cgywin64 termianl，并进入到软件版本包路径中，运行如下命令：
        $ bash win-checkimg.sh
       即可，等待脚本运行完成。
